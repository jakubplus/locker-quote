<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Kalkulator paczki{% endblock %}</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        form { background: #f7f7f7; padding: 20px; border-radius: 12px; width: 320px; }
        input { width: 100%; padding: 8px; margin: 5px 0; box-sizing: border-box; }
        button { width: 100%; padding: 10px; margin-top: 10px; background: #007bff; border: none; color: white; border-radius: 5px; cursor: pointer; }
        button:hover { background: #0056b3; }
        .result, .error { margin-top: 20px; padding: 15px; border-radius: 8px; }
        .result { background: #e8f5e9; border: 1px solid #81c784; }
        .error { background: #ffebee; border: 1px solid #ef5350; }
        .hint { color:#666; font-size: 12px; }
    </style>
    {% block head %}{% endblock %}
</head>
<body>
<h1>{% block heading %}Kalkulator paczki – Locker Quote{% endblock %}</h1>
<main>
    {% block body %}{% endblock %}
</main>
</body>
</html>
